#cloud-config

# Simplified cloud-init for testing Odoo deployment

# Update packages
package_update: true
package_upgrade: true

packages:
  - git
  - docker.io
  - docker-compose
  - curl
  - awscli
  - python3-pip          # ✅ NUEVO: Para instalar odoorpc
  - python3-requests     # ✅ NUEVO: Para hacer requests HTTP

# Setup and deploy Odoo
runcmd:
  # Setup Docker Compose v2 if not available
  - |
    if ! command -v docker-compose &> /dev/null; then
      curl -L "https://github.com/docker/compose/releases/download/v2.20.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
      chmod +x /usr/local/bin/docker-compose
    fi

  # Clone the Odoo repository
  - cd /root
  - git clone -b ${github_branch} https://${github_token}@${github_repo} odoo
  - cd odoo

  # Create .env file with configuration
  - |
    cat > /root/odoo/.env << 'EOF'
    # Odoo Configuration
    ODOO_ADMIN_PASSWORD=${odoo_admin_password}
    ODOO_DB_USER=odoo
    ODOO_DB_PASSWORD=${odoo_db_password}
    ODOO_DB_NAME=${odoo_db_name}

    # SSO Configuration
    ODOO_JWT_SECRET=${odoo_jwt_secret}
    SSL_ENDPOINT_API_KEY=${ssl_endpoint_api_key}

    # Odoo URLs
    ODOO_BASE_URL=${odoo_base_url}
    ODOO_PUBLIC_URL=https://${domain_name}

    # Orbit Integration
    BACKEND_URL=${backend_url}
    FRONT_URL=${frontend_url}

    # PostgreSQL Configuration
    POSTGRES_DB=postgres
    POSTGRES_USER=odoo
    POSTGRES_PASSWORD=${odoo_db_password}

    # Domain Configuration
    DOMAIN_NAME=${domain_name}

    # ✅ NUEVO: Orbit Webhook Configuration
    ORBIT_WEBHOOK_URL=${orbit_webhook_url}
    ORBIT_WEBHOOK_SECRET=${orbit_webhook_secret}
    TENANT_NAME=${tenant_name}
    EOF

  # Configure AWS CLI credentials for S3 access
  - mkdir -p /root/.aws
  - |
    cat > /root/.aws/credentials << 'AWSCREDS'
    [default]
    aws_access_key_id = ${aws_access_key}
    aws_secret_access_key = ${aws_secret_key}
    AWSCREDS
  - |
    cat > /root/.aws/config << 'AWSCONFIG'
    [default]
    region = ${aws_region}
    AWSCONFIG

  # Download template database from S3
  - aws s3 cp s3://odoo-templete-db-s3/odoo_template_03_12_25.zip /tmp/odoo_template_03_12_25.zip

  # Start services
  - cd /root/odoo
  - docker-compose up -d

  # Wait for Odoo to be fully ready (check from inside the container)
  - |
    echo "Waiting for Odoo to be ready..."
    max_attempts=60
    attempt=0
    until docker exec odoo-odoo-1 curl -s http://localhost:8069/web/database/manager > /dev/null 2>&1 || [ $attempt -eq $max_attempts ]; do
      echo "Waiting for Odoo... attempt $attempt/$max_attempts"
      sleep 5
      attempt=$((attempt + 1))
    done
    echo "Odoo is ready!"

  # Copy backup file into Odoo container
  - docker cp /tmp/odoo_template_03_12_25.zip odoo-odoo-1:/tmp/odoo_template_03_12_25.zip

  # Restore database using Odoo's HTTP API (from inside the container)
  - |
    docker exec odoo-odoo-1 curl -X POST \
      -F "master_pwd=${odoo_admin_password}" \
      -F "name=${odoo_db_name}" \
      -F "backup_file=@/tmp/odoo_template_03_12_25.zip" \
      http://localhost:8069/web/database/restore

  # Fix nginx proxy issue automatically
  - sleep 10
  - docker-compose -f /root/odoo/docker-compose.yml up -d --force-recreate nginx

  # ✅ NUEVO: Install odoorpc in the Odoo container
  - echo "📦 Installing odoorpc in Odoo container..."
  - docker exec odoo-odoo-1 pip3 install --break-system-packages odoorpc

  # ✅ NUEVO: Install odoorpc on host for the setup script
  - echo "📦 Installing odoorpc on host..."
  - pip3 install odoorpc

  # ✅ NUEVO: Setup Orbit Integration
  - echo "🔧 Setting up Orbit integration..."
  - |
    # Export variables for Python script directly (don't source .env to avoid parsing issues)
    export ODOO_DB_NAME="${odoo_db_name}"
    export ODOO_ADMIN_PASSWORD="${odoo_admin_password}"
    export ODOO_ADMIN_EMAIL="${odoo_admin_email}"
    export TENANT_NAME="${tenant_name}"
    export ODOO_PUBLIC_URL="https://${domain_name}"
    export ORBIT_WEBHOOK_URL="${orbit_webhook_url}"
    export ORBIT_WEBHOOK_SECRET="${orbit_webhook_secret}"

    # Wait for Odoo API to be ready and accepting connections
    echo "⏳ Waiting for Odoo API to be ready..."
    max_attempts=60
    attempt=0
    while [ $$attempt -lt $$max_attempts ]; do
      # Check if Odoo is responding (any 2xx or 3xx response is good)
      http_code=$$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8069/ 2>/dev/null)
      if [ "$$http_code" -ge 200 ] && [ "$$http_code" -lt 400 ]; then
        echo "✅ Odoo API is ready (HTTP $$http_code)!"
        sleep 5  # Extra 5 seconds to ensure DB is fully accessible
        break
      fi
      echo "Waiting for Odoo API... attempt $$attempt/$$max_attempts (HTTP $$http_code)"
      sleep 5
      attempt=$$((attempt + 1))
    done

    if [ $$attempt -eq $$max_attempts ]; then
      echo "❌ Timeout waiting for Odoo API"
      exit 1
    fi

    # Execute Orbit setup script
    echo "🚀 Executing Orbit integration setup..."
    python3 /root/setup_orbit_integration.py > /var/log/orbit-setup.log 2>&1

    # Check result
    if [ $? -eq 0 ]; then
      echo "✅ Orbit integration setup completed successfully"
      echo "📋 Check details: /var/log/orbit-setup.log"
    else
      echo "❌ Orbit integration setup failed"
      echo "📋 Check errors: /var/log/orbit-setup.log"
    fi

# Write helper files
write_files:
  - path: /root/odoo-info.txt
    content: |
      Odoo Server Information
      =======================
      Domain: ${domain_name}
      Tenant: ${tenant_name}
      Branch: ${github_branch}

      Check status: docker-compose -f /root/odoo/docker-compose.yml ps
      View logs: docker-compose -f /root/odoo/docker-compose.yml logs -f
      Orbit setup log: cat /var/log/orbit-setup.log

  # ✅ NUEVO: Python script for Orbit Integration setup
  - path: /root/setup_orbit_integration.py
    permissions: '0755'
    content: |
      #!/usr/bin/env python3
      import odoorpc
      import requests
      import sys
      import os
      import subprocess

      print("\n" + "="*60)
      print("  ORBIT INTEGRATION SETUP")
      print("="*60 + "\n")

      # Validate environment variables
      required_vars = ['ODOO_DB_NAME', 'ODOO_ADMIN_PASSWORD', 'ODOO_ADMIN_EMAIL', 'TENANT_NAME',
                       'ODOO_PUBLIC_URL', 'ORBIT_WEBHOOK_URL', 'ORBIT_WEBHOOK_SECRET']
      missing = [v for v in required_vars if not os.environ.get(v)]

      if missing:
          print(f"❌ Missing environment variables: {', '.join(missing)}")
          sys.exit(1)

      odoo_db = os.environ['ODOO_DB_NAME']
      odoo_password = os.environ['ODOO_ADMIN_PASSWORD']
      admin_email = os.environ['ODOO_ADMIN_EMAIL']
      tenant_name = os.environ['TENANT_NAME']
      odoo_url = os.environ['ODOO_PUBLIC_URL']
      webhook_url = os.environ['ORBIT_WEBHOOK_URL']
      webhook_secret = os.environ['ORBIT_WEBHOOK_SECRET']

      try:
          # Connect to Odoo
          print(f"📡 Connecting to Odoo (DB: {odoo_db})...")
          odoo = odoorpc.ODOO('localhost', protocol='jsonrpc', port=8069)
          odoo.login(odoo_db, admin_email, odoo_password)
          print(f"✅ Connected to Odoo as {admin_email} (UID: {odoo.env.uid})")

          # Create API Key
          print("🔑 Creating API Key...")
          ApiKey = odoo.env['res.users.apikeys']
          existing = ApiKey.search([('name', '=', 'Orbit Integration'), ('user_id', '=', odoo.env.uid)])

          if existing:
              print(f"✅ Found existing API Key (ID: {existing[0]})")
              # Get key from database (key field is not readable via API for security)
              result = subprocess.run(
                  ['docker', 'exec', 'odoo-db-1', 'psql', '-U', 'odoo', '-d', odoo_db, '-t', '-c',
                   f"SELECT key FROM res_users_apikeys WHERE id = {existing[0]};"],
                  capture_output=True, text=True, check=True
              )
              api_key = result.stdout.strip()
          else:
              # Create new key via API (respects Odoo permissions and validations)
              key_id = ApiKey.create({'name': 'Orbit Integration', 'user_id': odoo.env.uid})
              print(f"✅ Created new API Key (ID: {key_id})")

              # Get key from database
              result = subprocess.run(
                  ['docker', 'exec', 'odoo-db-1', 'psql', '-U', 'odoo', '-d', odoo_db, '-t', '-c',
                   f"SELECT key FROM res_users_apikeys WHERE id = {key_id};"],
                  capture_output=True, text=True, check=True
              )
              api_key = result.stdout.strip()

          # Send webhook
          print(f"📤 Sending webhook to Orbit...")
          response = requests.post(
              webhook_url,
              json={
                  'tenant_name': tenant_name,
                  'odoo_url': odoo_url,
                  'database': odoo_db,
                  'api_key': api_key,
                  'webhook_secret': webhook_secret
              },
              timeout=30
          )
          response.raise_for_status()

          print(f"✅ Webhook sent successfully: {response.status_code}")
          print("\n" + "="*60)
          print("  SETUP COMPLETED SUCCESSFULLY")
          print("="*60)
          print(f"✅ Tenant: {tenant_name}")
          print(f"✅ URL: {odoo_url}")
          print(f"✅ Database: {odoo_db}")
          print(f"✅ API Key: {api_key[:15]}...")
          print("\n🎉 Orbit integration is ready!\n")
          sys.exit(0)

      except subprocess.CalledProcessError as e:
          print(f"\n❌ Database error: {e}")
          if e.stderr:
              print(f"Stderr: {e.stderr}")
          sys.exit(1)
      except Exception as e:
          print(f"\n❌ ERROR: {e}")
          import traceback
          traceback.print_exc()
          sys.exit(1)

final_message: |
  ============================================
  Odoo server deployment started!
  ============================================
  Domain: ${domain_name}
  Tenant: ${tenant_name}

  Check deployment status:
  cd /root/odoo && docker-compose ps
  ============================================
